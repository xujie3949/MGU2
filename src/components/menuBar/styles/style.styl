.container {
  position: relative;
  width: 100%;
  height: 36px;
  line-height: 21px;
  border-bottom: 1px solid rgba(0, 0, 0, 0.45)
  padding: 0;
  margin: 0;
  overflow: visible;
}

.container button:focus {
  outline: none;
}